import ProtectedRoute from "@components/ProtectedRoute";

const CartPage = () => {
    return (
        // <ProtectedRoute>
            <div>Cart page</div>
        // </ProtectedRoute>
    );
};

export default CartPage;
