import ProtectedRoute from "@components/ProtectedRoute";

const MarketPage = () => {
    return (
        <ProtectedRoute>
            =<div>Market page</div>
        </ProtectedRoute>
    );
};

export default MarketPage;
